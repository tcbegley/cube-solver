[build-system]
requires = [ "setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
target-version = [ "py36" ]

[tool.isort]
include_trailing_comma = true
line_length = 79
multi_line_output = 3
